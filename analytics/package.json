{
  "name": "analytics",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "remote": "wrangler dev --remote",
    "start": "node --env-file .dev.vars local.js"
  },
  "devDependencies": {
    "puppeteer": "^21.6.0",
    "wrangler": "^3.19.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.5",
    "graphql-request": "^7.1.0"
  }
}
